export class Project {
  constructor(
    public id: string,
    public name: string,
    public image: string,
    public thumbnail: string,
    public language: string,
    public description: string
  ) {
  }
}
