import { SlideContentDirective } from './slide-content.directive';

describe('SlideContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideContentDirective();
    expect(directive).toBeTruthy();
  });
});
